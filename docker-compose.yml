services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: apip
      POSTGRES_PASSWORD: apip_secret
      POSTGRES_DB: apip
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
